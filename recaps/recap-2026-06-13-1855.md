# Recap — 2026-06-13 session: Discord freeze root-cause + no-JIT wall

Self-contained chronological flow. Project: WebKit (WebCore+JSC) compiled to
wasm32, no-JIT CLoop, running in a browser tab; Skia GPU canvas; Wisp networking;
engine on an Emscripten pthread.

---

**1. Session open — `/compact midtask` + "recap from the handoff".**
I re-oriented from the active handoff (`handoff-2026-06-13-timer-js-pacing.md`):
shipped last session = present pacing (581bc71) + dynamic render throttle (1ed013b);
open = task #77 "pace bib_pump timer/microtask JS" to cut residual Discord freezes
("pumpGap=700–933ms/s"). HEAD=820316b.

**2. User: "id say look to everything including 77 and js perf".**
I surveyed the perf landscape. Read main.cpp's tick/pump model: `bib_tick` (paints),
`bib_pump` (RunLoop::cycle only — timers/microtasks, NO paint), `bib_pump_network`
(curl hostPump + cycle). Found `bibWakeUp` ALREADY coalesces (engine-pre.js). Found
JSC builds -O3 already; blocklist is 3rd-party-only; #57 toObjectSlow is mitigated.
Added perflog-gated per-cycle instrumentation to `bib_pump`/`bib_pump_network`
(`pump=T(maxM nN) net=...`) in main.cpp; rebuilt. Measured Discord: **steady-state
is HEALTHY (GPU ~50fps, ~50% busy); the freezes are monolithic single RunLoop
cycles** (`JS=1637`, `net=846`), and `bib_pump` timer JS is tiny. **This overturned
#77's premise** — the old "pumpGap" was the page-side formula mislabeling
`bib_pump_network` cycles. Asked the user for direction.

**3. User chose "Capture logged-in profile" + pasted a logged-in trace.**
[large paste: logged-in BIBPERF trace] It showed `RangeError: Out of memory` then
6–10s freezes. I pivoted to a memory hypothesis: added a heap gauge
(`emscripten_get_heap_size()` + `commonVM().heap.size()` → `heap=MB jsc=MB`),
rebuilt. Headless baseline: **engine is lean — 369MB flat, NO leak**, present path
leak-clean. So OOM looked like Discord's own footprint.

**4. User pasted an abort trace: "i cant get into discord anymore".**
[large paste: trace ending in `Aborted()` at heap=442MB, painted=0] Diagnosed: NOT
OOM (442MB low) — a crash, almost certainly corrupt persisted profile from the
earlier hard crash. Found the unblock: **`?persist=clear`** (verified IDB is
in-memory at main.cpp:2041, so only `bib-state-v1.json` persists). Explained two
distinct failures: OOM-freeze vs boot-abort.

**5. User clarification: "i was running it with ...?gpu=1&perflog=1...".**
Confirmed persistence was on (restored session) — consistent with the corrupt-blob theory.

**6. User chose "Capture split + I build shed" + pasted a clean logged-in trace WITH the heap gauge.**
[large paste: logged-in trace with heap/jsc] **THE DECISIVE FINDING:** the 10.5s
freeze is at `heap=531MB / 4GB, jsc=102MB` — **memory is FINE**. It's a single
synchronous `RunLoop::cycle()` (`pump=10525 max=10481`) hydrating Discord's gateway
READY. **Overturned the memory theory too** (the first trace's OOM was profile bloat).
Root cause = no-JIT object hydration. I declined to build the memory shed (wrong fix),
explained none of pacing/memory/caches apply, and recommended a GPT-5.5 consult.

**7. User chose "GPT-5.5 creative consult". (Granted.)**
Wrote `build/gpt55-discord-nojit-brief.md`, launched GPT-5.5 (background). Capture
truncated to a fragment ("large_threshold = 25") 3×; recovered via `--continue` with
plain-text forcing. Saved verbatim → `docs/research/research-04-discord-nojit-consult.md`.
**Synthesis (mine, not a relay):** GPT-5.5 agreed it's the no-JIT wall; ranked payload
reduction (large_threshold/client_state) #1, then UX framing, then conditional native
accelerators. My corrections: (a) GPT-5.5 said "proxy-rewrite Identify" but TLS
terminates in-engine → must be a WebCore WS-send hook; (b) elevated persistent-IDB
(IDB is in-memory → Discord cold-identifies every launch); (c) native-decompression
likely low payoff (object hydration dominates). Asked direction.

**8. User chose "Measurement + UX overlay".**
Built both HOST-SIDE in web/browser.html (no rebuild): **#78** per-host wisp byte
accounting + perflog `wisp bytes/host:` dump; **#79** freeze overlay (watchdog on
`__bib.frames`, spinner CSS animating on the host thread during the engine freeze,
input-gated to avoid idle false-positives). Codex review: 2 findings fixed
(document-level input listeners; `Object.create(null)`). gate2 + gate8 PASS.

**9. User: "make it always non site specific just the generic working/loading" + pasted logs + "is it *theoretically* possible to get jit?"**
[large paste: logged-in logs] Made overlay generic ("Loading…"/"Working…"/"Engine
stopped"). **Key measurement from the logs: `gateway.discord.gg = 0.8MB`** — the
payload is TINY, so the freeze is compute-bound hydration (0.8MB → ~95MB objects →
10.8s), and **payload-reduction levers are weak**. Answered the JIT question:
traditional native JIT impossible (wasm W^X); **JIT-to-wasm** theoretically possible
(emit wasm at runtime via `WebAssembly.instantiate`, browser compiles it) but
research-scale + per-call boundary cost may not amortize object churn; host-JS
sidecar rejected (isolation). JIT is THE fix → why it's the wall.

**10. User: "have 5.5 pro deep analyise jit along with up to 10 core files then make handoff and ill compact".**
Launched GPT-5.5 deep JIT consult #2 (background, task #80) with 10 files attached
(CLAUDE.md, decision-001, decision-006, 00-brief, build-webcore.sh, embedder.cmake,
main.cpp, wasm-polyfill.js, engine-pre.js, research-04). Wrote the new active handoff
`docs/summaries/handoff-2026-06-13-discord-nojit-wall.md`. Wrote this recap. Consult
#2 likely returns post-compact (→ save to research-05, synthesize).

---

**State at recap:** HEAD=820316b. Uncommitted: M main.cpp (BIBPERF instrumentation,
in live binary), M browser.html (#78/#79, Codex-clean, gates pass), M timer-js handoff
(superseded banner), ?? research-04. Memory updated (discord-freeze-rootcause-nojit
NEW). Tasks: #78/#79 done, #77 root-caused, #80 = pending JIT consult. Bottom line:
**no-JIT wall; Discord usable (one-time ~10s sync then ~50fps); overlay makes it
legible; real fix = JIT (pending #80) or accept.**
