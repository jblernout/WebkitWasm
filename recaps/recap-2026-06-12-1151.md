# Recap — 2026-06-12 11:51 (post-compact reconstruction of overnight session)

Note: written from the compact summary immediately after `/compact midtask`;
the pre-compact original transcript is in the session jsonl. Covers the
overnight autonomous session (4 commits, 0caafd9 → de413be).

## Conversation flow

**User:** "ok recap what we are left with" (post-compact)
**Assistant:** Delivered recap of handoff queue: request blocklist → curldebug
NetworkError chase → WS-1 real WebSockets → Tier A2 media, then backlog.

**User:** `/model` → Fable 5.

**User:** "Okay begin working in the best order you belive to gett better site
support and hope fully fix discord along with the general improvements
mentiomed"
**Assistant:** Executed the full committed queue autonomously over the night
(background builds + ScheduleWakeup loop ticks):

1. **Request blocklist (#60, commit 0caafd9)** — ~35 tracker/analytics host
   suffixes blocked at EmbedderLoaderStrategy::loadResource (the single
   subresource choke point); beacons complete-as-success in startPingLoad;
   `?noblock=1` toggle; MainResource exempt. Measured: tekeye 14× adsbygoogle
   blocked, wispStreams 8→6, paint identical. Discord /login: ZERO blocklisted
   hosts — general win, non-factor for the login peg. Codex review applied
   (facebook.net removed, strict ===\"1\" toggle, bat.bing.com etc. added).

2. **NetworkError chase closed (#61)** — DEBUG_CURL full trace (9,689 lines):
   518/518 discord.com requests succeed over 3 H2 connections, zero curl
   errors. `NetworkError: Load failed` correlates 3/3 with Discord's
   libdiscore wasm import failing (our wasm2js bridge rejects it as
   untranslatable; one consumer doesn't catch). COSMETIC, non-blocking.
   Tooling added: BIB_CURLDEBUG + BIB_STDERR_FILE on site-diagnose.

3. **WS-1 real WebSockets (#58, commit ede840e)** — BibWebSocketChannel.h
   (~500 lines): ThreadableWebSocketChannel + CurlStream::Client, protocol
   ported from WebKit2's WebSocketTaskCurl. CurlStream Emscripten fail-fast
   stub replaced with non-blocking multi-driven CONNECT_ONLY connect. Six
   curl/wasm traps hit and recorded in playbook (multi owns connection cache;
   ACTIVESOCKET -1 as success → FD_SET OOB corruption; CURLMcode check; etc).
   wsecho 6/6 vs echo.websocket.org; Discord QR remote-auth-gateway socket
   CONNECTS (wispStreams 3→4). Also #57 toObjectSlow instrumentation deployed.

4. **Tier A2 real media elements (#62, commit f1592cc)** — ENABLE_VIDEO=ON
   with zero media engines → real HTMLMediaElement, spec-correct failures
   (canPlayType()=\"\", play()→NotSupportedError, error event code 4). Hit the
   WEBKIT_OPTION cache trap a 3rd time (silent no-op build; fixed with
   explicit -DENABLE_VIDEO=ON + cmakeconfig.h verify). audioprobe 12/12.

5. **W-B scoping (#63, commit de413be)** —
   docs/summaries/analysis-wb-engine-off-main-thread.md: GO, phased,
   confidence medium-high. Engine → pthread via -pthread + PROXY_TO_PTHREAD;
   single-mutator rule kept (one engine thread). Phases W-B0 spike → W-B1
   raster (acceptance: /login boots with responsive host tab) → W-B2 GPU+G4
   → W-B3 dividends. Load-bearing unknown: SOCKFS WebSocket placement under
   PROXY_TO_PTHREAD.

**User:** "whats been done and implemented this session thus far" → status
answer mid-A2-build.

Session closed deliberately: push notification sent, loop not rescheduled,
recommendation = fresh session for W-B0. All gates (2/3/8/9) green, 4-site
regression sweep clean, handoff updated with new opener block.

**User:** `/compact midtask`, then: "ok recap what we got done and whats left
and why many sites are still super slow or dont work like discord logins,
videos not giving error but not loading etc" → this file + in-chat answer:
slowness = structural (CLoop interpreter, no JIT, engine on host main
thread → W-B); videos = A2 by design (no media engines, spec-correct
NotSupportedError; playback needs a future decoder/MSE phase).

## State
- main @ de413be, tree had probe-font experiments + minor diffs uncommitted
  at session start snapshot (pacifico.woff2, webfont.html, etc).
- Dev server :8080 + wisp :5001 up.
- Next opener: W-B0 pthread spike (kill criteria in analysis doc).
- #57 instrumentation live, awaiting recurrence.
