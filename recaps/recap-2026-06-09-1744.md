# Recap — 2026-06-09 session (project kickoff → Phase 1 complete → Phase 2 started)

Self-contained chronological record. Reader needs zero prior context.

---

## Turn 1 — Project goal (user, verbatim)
> Project goal: The goal is to port a browser within the browser, Seriously, The options are chrome gecko or webkit, I want a fully modern version and we will port it so the user has an interactable canvas that acts like the renderer for what would be the browser the full browser and engine runs within the browser and for network we will use a "wisp server" (ie wisp-js, wisp-server-python) doesnt matter

**Assistant**: Assessed feasibility honestly (direct source-port of a major
engine is frontier-hard; emulation routes exist). Presented 4 strategies
via option picker: QEMU-wasm VM, v86 JIT VM, CheerpX, true WebKit→WASM
source port; and 3 engines.

## Turn 2 — User chose (via option picker)
**Strategy: WebKit-to-WASM source port** (hardest, no VM, engine IS the
wasm). **Engine: WebKit.**

## Turn 3 — "continue"
**Assistant**: Scaffolded repo (local git, CLAUDE.md, docs structure),
wrote `docs/summaries/00-project-brief.md` (7 fail-fast phases, risk
register) and `decision-001-architecture.md`. Launched two research
agents. Results synthesized into `decision-002-toolchain-pins.md`:
- Base: JSCOnly first → custom PORT=Emscripten modeled on PlayStation port
  (curl + generic RunLoop + Skia, no glib). Trimmed WPE rejected.
- Skia CPU raster only (cairo dead in WebKit ≥2.46); wisp-js 0.4.1 server
  (wisp-server-python rejected — Wisp v1 only); wasm32 (Memory64 rejected);
  `-pthread -sPROXY_TO_PTHREAD`, no Asyncify/JSPI; emsdk 6.0.0; WebKit
  pinned `webkitglib/2.52` @ `aec9d2ad9` (full clone, tarballs incomplete).
- Networking de-risked by ading2210/libcurl.js precedent: curl+mbedTLS on
  Emscripten SOCKFS with wisp-js WispWebSocket swap — pure JS shim.
- Key risk: NO supported single-process embedding API outside Cocoa —
  embed WebCore::Page against internal headers; Phase 2 = make-or-break.

Then built Phase 0 infra: COOP/COEP dev server (`tools/dev-server.mjs`,
Codex-reviewed, symlink/streaming/EADDRINUSE fixes), wisp relay verified
(`npm run wisp` → 127.0.0.1:5001), emsdk 6.0.0 + WebKit clone + ruby
installed; ICU 77.1 cross-compiled (3 quirks: mh-unknown is an error-stub
that must be force-overwritten with mh-linux; static data packaging
impossible — genccode needs ELF — use archive packaging; .dat preloaded).

**Phase 1 build loop** (`tools/build-jsc.sh`, ninja -k 50, patches
exported to `src/patches/webkit-emscripten.patch` via
`tools/export-webkit-patches.sh`): ~10 WebKit patches total —
FileHandle.h ino_t (OS(LINUX)→OS(UNIX)); offlineasm must scan the .wasm
not the .js stub + extractors linked -O0 (Binaryen memory-packing trims
zero-tail data segments); RawHex.h + Options.cpp parse<Size> + MathCommon
roundeven guards extended with __EMSCRIPTEN__ (wasm32 is Darwin-like:
size_t=unsigned long≠unsigned int); dl_iterate_phdr → __TIMESTAMP__
fallback; sigsuspend compiled out. Link fixes: STACK_SIZE=8MB (64KB
default caused empty-message StackOverflowError on EVERYTHING),
INITIAL_MEMORY=128MB, ALLOW_MEMORY_GROWTH, MAXIMUM_MEMORY=4GB,
--embed-file ICU .dat at its compiled-in absolute path.

**GATE 1 PASSED both halves**: node `print(6*7)` → 42; headless Chromium
(Playwright) `JSC-in-tab: 42`, crossOriginIsolated=true.

## Turn 4 — "set any memorys up now …"
**Assistant**: Wrote persistent memories:
`browserinbrowser-architecture` (decisions + status) and
`webkit-wasm-porting-playbook` (all fix patterns + gotchas), indexed in
MEMORY.md. Meanwhile diagnosed the stack-size root cause (above).

## Turn 5 — Codex patch review (background) returned
1 critical: GC's MachineThreads::tryCopyOtherThreadStacks calls
Thread::suspend → could DEADLOCK (pthread_kill delivery not guaranteed)
before reaching the handler trap. Fixed: deterministic
RELEASE_ASSERT at suspend() ENTRY. **Hard constraint recorded: ONE
MUTATOR THREAD PER VM** (Web Workers safe — own VM each;
ENABLE_SAMPLING_PROFILER stays OFF). Both gates re-verified green.

## Turn 6 — User question (verbatim)
> even if phase 2 fails ( dont worry claude i belive in you ) what can phase 1 be used for realisticly does something like that already exist?

**Assistant**: Only direct precedent is mbbill/JSC.js (2019, dead, no
ICU/threads); ours is likely the only CURRENT flagship-engine JS VM
running in a browser tab. Standalone uses: untrusted-JS sandboxing
(Figma-pattern, incl. LLM-generated code), deterministic cross-client
execution, JSC playground/differential testing, embeddable scripting
engine for other wasm apps (libJavaScriptCore.a), upstreamable patch set.

## Turn 7 — Phase 2 started
Full WebCore dependency tier built to `third_party/wasm-sysroot` IN ONE
RUN (`tools/build-deps/webcore-deps.sh`): zlib, libpng, libjpeg-turbo,
libwebp, freetype, harfbuzz(+icu), libxml2, sqlite3 — static, -pthread.
(Only hiccup: zlib.net deletes old tarballs → GitHub release URL.)
Port-surface research written to
`docs/research/research-03-port-skeleton-map.md` (ALL_PORTS at
WebKitCommon.cmake:42-52; minimal port = Options + WTF/WebCore/PAL
Platform files + 1-line edit). Active handoff:
`docs/summaries/handoff-2026-06-09-phase2-port-skeleton.md`.
**Background agent launched**: drafting PORT=Emscripten skeleton +
build-webcore.sh + first configure probe (configure-stage only; curl
find_package failure EXPECTED — curl/mbedTLS tier not built yet).

## Turn 8 — User (verbatim)
> btw get to a stopping point or a "compaction ready" point soon once thats back so i can compact and we resume

**Assistant**: Wrote this recap; finalizing when the skeleton agent lands.

---

## Resume-from-here cheat sheet
- Git: 13+ commits, all local, tree clean as of `c212243`.
- Run gates: `cd build/jsc/bin && node jsc.js -e 'print(6*7)'`;
  browser: `npm run dev` + `node tools/gate1-browser-test.mjs`.
- Next task: review/commit the port-skeleton agent output, then iterate
  WebCore configure → build curl tier (libcurl.js recipes) → first paint.
- Constraints + gotchas: memory `webkit-wasm-porting-playbook`; pins in
  `00-project-brief.md`; ONE active handoff file (above).

## Agent result addendum (filled after agent returned)
PORT=Emscripten skeleton COMPLETE and committed: OptionsEmscripten.cmake
(JSCOnly base + USE_SKIA/USE_CURL/generic event loop, all extras OFF),
Platform files for WTF/JSC/WebCore/PAL, ALL_PORTS edit,
tools/build-webcore.sh. Configure probe resolves ALL nine sysroot deps
and stops exactly at the documented gap: `Could NOT find CURL`. Queued
behind it: OpenSSL → LibPSL → Fontconfig (vendored Skia requires it).
Patch ledger 472 lines/14 diffs (exporter now --intent-to-add for new
files). KEY FINDING amending decision-002: WebCore's curl backend
hardwires the OpenSSL API — build curl against OpenSSL/BoringSSL for
wasm, NOT mbedTLS (libcurl.js's mbedTLS recipe applies only to the
SOCKFS→Wisp shim pattern). Open decisions for next session: OpenSSL vs
BoringSSL; Fontconfig build vs Skia fontmgr patch; TextureMapper
verify-at-compile. All in the updated handoff.
