# Recap — 2026-06-10 23:50 (wave-2 sweep: every verdict improved)

Continues from recap-2026-06-10-2330.md (post-compact continuation).
Commits this session: **e48bd55** (sweep tooling channel knob) on top of
2d02f6d → 102c655. Tip: e48bd55.

## Turn 1
**User:** "okay so regarding perf and site support whats on the table"
*(after /compact midtask)*

**Assistant:** Presented the handoff menu with costs/unlocks:
1. Wave-2 real-site sweep (#30) — cheap, calibrates everything.
2. Web Workers epic — biggest unlock (Discord/Turnstile/reCAPTCHA
   regular variant) but requires -pthread full rebuild + ledger
   re-audit; scope first.
3. Page-level WebSocket (CurlStream multi-interface rework).
4. WebGL (#32) — investigation spike needed before sizing.
5. Dirty-rect paint/blit (#37) — biggest steady-state CPU win.
6. CPU profile of bib_tick.
7. Phase 5 chrome (secondary).
Recommended sweep-first. Asked via AskUserQuestion.

## Turn 2
**User (AskUserQuestion answer):** "start with 1 but all the others are
on the table still"

**Assistant:** Ran the wave-2 sweep (task #39):
- Added **BIB_CHANNEL** env knob to tools/site-diagnose.mjs and
  tools/google-login-repro.mjs (full-chromium channel routes around the
  headless-shell V8 SEGV, #38). Committed e48bd55. Zero host crashes
  during the whole sweep.
- **site-diagnose pass** (fal.ai/grants, accounts.google.com,
  old.reddit, velzie.rip, 2captcha recaptcha-v2 demo) under systemd-run
  8G scope → build/sweep2-pass1.log + screenshots build/diagnose-*.png.
- **google-login full flow** (tools/google-login-repro.mjs 600 134):
  email types visibly, Enter → REAL "Couldn't find your Google Account"
  error rendered in **≤3s**. The never-resolving gray overlay is dead.
- **reCAPTCHA click-feel** (new throwaway build/recaptcha-click.mjs,
  gitignored): clicks the checkbox, polls a 36×36px framebuffer hash
  around it → **first visual response +0.46s**; the full image
  challenge (3×3 "select all buses" grid, every tile painted) renders.
  Worker NotSupportedError fires on load AND +2.3s post-click — the
  challenge path wants Workers; still the suspected fallback trigger.

**Verdict table (all improved, NO aborts, NO crashes):**
- old.reddit: FLIPPED — real front page (941 colors, 7 streams, zero
  errors); the server-side "network policy" block page is gone.
- google login: FULL FLOW PASS (≤3s round trip).
- reCAPTCHA: click-feel FIXED (0.46s), challenge renders.
- velzie.rip: near-perfect; ONLY WebGL background dead (gl.viewport
  null, #32). Button-wall = environmental (ad-block DNS + rinici.de).
- fal.ai/grants: renders fine; minor purple-noise artifact in their
  animated hero canvas (unexplored).
- Only 2 repeated engine gaps across all 5 sites: **Workers** (×2),
  **WebGL** (×2). Everything else environmental.

Conclusion drawn: pump #17 solved the *feel* problem (0.46s click
response); dirty-rect re-classified as efficiency win. Workers is now
clearly the #1 site-support lever. Recommended Workers scoping pass.

## Turn 3
**User (AskUserQuestion answer):** "build a handoff to start with 1
keep the others in mind so i can compact after we have refreshed our
checkups" *(1 = Workers scoping, the recommended option)*

**Assistant:** Wrap executed:
- Task #39 completed; **task #40 created**: Workers scoping pass —
  go/no-go decision doc, NO build flip. Checklist in the task: pthread
  flag impact, ledger single-threaded-surgery inventory, per-worker
  VM/heap cost (wasm32 4GB), per-site unlock truth-table, phasing.
- Handoff (docs/summaries/handoff-2026-06-10-phase5-chrome.md): NEXT
  SESSION = Workers scoping (task #40); wave-2 verdicts recorded; menu
  re-ranked (sweep struck through, Workers marked CURRENT).
- Memory browserinbrowser-architecture.md: sweep status block appended;
  #38 cross-check resolution (full chromium 8/8 clean) recorded.
- This recap.

## Environment for resume
dev :8080 + wisp :5001 running. Binary build/webcore/bin/embedder.wasm
~106MB CLEAN, all gates green. Tree clean at e48bd55; WebKit tree ==
patch ledger. Sweep artifacts (gitignored): build/sweep2-*.log,
build/recaptcha-click.mjs + recaptcha-*.png, build/google-login-*.png,
build/diagnose-*.png. systemd-run scopes mandatory (12G build / 8G
browser); BIB_CHANNEL=chromium for heavy-JS site runs. NEXT: task #40
Workers scoping pass (handoff "⟶ NEXT SESSION STARTS HERE").
