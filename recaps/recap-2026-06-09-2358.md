# Recap — 2026-06-09 late session (embedder → PHASE 2 GATE PASSED)

Self-contained chronological record. Prior session: recaps/
recap-2026-06-09-2258.md (curl tier → libWebCore.a compiles). Reader needs
zero prior context beyond: this project compiles WebKit to wasm
(PORT=Emscripten, single-process embedder, Skia CPU raster), and at session
start WebCore compiled but nothing had ever run.

---

## Turn 1 — Session resumed from compaction ("continue task")
**Assistant** executed the Phase 2 embedder attack plan end-to-end:

**Build wiring (decision)**: embedder sources live in `src/embedder/` (our
repo); a 3-line hook in WebCore/PlatformEmscripten.cmake —
`cmake_language(DEFER CALL include "${EMSCRIPTEN_EMBEDDER_CMAKE}")` — runs
`src/embedder/embedder.cmake` at WebCore directory-end where the WebCore
target + FINAL include lists exist. Include dirs copied per the
WebCoreTestSupport pattern (PRIVATE dirs don't propagate).

**Embedder written** (`src/embedder/main.cpp`): canonical init
(JSC::initialize → initializeMainThread → setProcessPrivileges →
initializeCommonAtomStrings → install strategies), then the
SVGImage::dataChanged sequence: pageConfigurationWithEmptyClients →
Page::create → LocalFrameView → init() → DocumentWriter
begin/addData/end → updateLayoutIgnorePendingStylesheets → render-tree
dump → GraphicsContextSkia paint (explicit kRGBA_8888 SkSurface) →
PPM to /out.ppm + region-pixel gate counts.

**Compile fixes**: WTFMove → WTF::move; 2.52 inline idiom (LocalFrame::view
body lives in DocumentView.h, protectedDocument in LocalFrameInlines.h).

**Link battle**: first link surfaced the predicted storm — full list
extracted with `-Wl,--error-limit=0`: **48 unique undefined symbols** in
the families the handoff predicted (PlatformScreen, Theme/RenderTheme/
ScrollbarTheme/SystemFontDatabase singletons, AX platform halves,
Pasteboard+Editor, MIMETypeRegistry, modifier keys, WebCrypto, GL).
Resolved with **12 stub files** under Source/WebCore/*/emscripten/
(cribbed from PlayStation + libwpe) + 1 guard join (Pasteboard.h
text/markup: `USE(LIBWPE) || defined(__EMSCRIPTEN__)`). One iteration:
GLContextWrapper.cpp looked self-contained but hard-includes ANGLE —
stubbed its dtor instead. **embedder.wasm linked: 87.6 MB** (embedded ICU
dat + DejaVuSans + etc/fonts staged from sysroot with broken
DESTDIR-relative symlinks dereferenced via conf.avail).

**Runtime discovery**: a PlatformStrategies is MANDATORY
(FrameLoader::pageLoadCompleted dereferences the loader strategy
unconditionally) → `src/embedder/EmbedderStrategies.cpp` (offline
LoaderStrategy, no-op MediaStrategy, null pasteboard/blob).

**KEY FINDING**: the ENTIRE stack (Phase 1 jsc + libWebCore + sysroot) was
built WITHOUT -pthread — single-threaded Emscripten mode. The embedder
matches. Threads = future whole-rebuild decision (~35 min), documented.

## Turn 2 — User: "how close are we currently to a working browser in browser tldr while you work"
**Assistant**: phase map TLDR — Phase 1 proven, 2a done, 2b ~90%, gate
imminent; if it passes, static-page browser-in-browser within days of
work, interactive after networking. (Link was running in background.)

## Turn 3 — THE GATE
embedder.wasm linked; two node-runner traps from the Phase 1 lessons hit
and fixed: (1) project package.json "type":"module" → ERR_REQUIRE_ESM
dumps the minified source — dropped {"type":"commonjs"} into
build/webcore/bin (now automated in build-webcore.sh); (2) global.Module
is shadowed by Emscripten's hoisted `var Module` under CJS → attach
preRun/onExit to module.exports AFTER require() (async wasm instantiation
makes that safe).

**RESULT — PHASE 2 GATE PASSED (~23:43)**: 800×600 frame, blue #0066CC
div EXACTLY 20000 px, antialiased #CC0000 "hello" in DejaVu Sans, white
elsewhere; render tree shows real shaped-text metrics (width 116). Image:
build/out.png. The full engine pipeline works in wasm: parser → style →
layout → fontconfig/FreeType/HarfBuzz → Skia raster.

**Codex review** (mandatory): 0 critical / 0 high / 2 medium / 3 low —
all five fixed (region-specific gate assertions so missing fonts can't
hide behind the div; PPM write error propagation; font-staging
completeness validation; CMake cache value re-sync; extensionsForMIMEType
implemented). Gate re-verified: exactBlue=20000, redGlyph=1541.

**Commits**: a680190 (links) → da01669 (GATE PASSED) → d7fb690 (docs) →
c8fbf59 (review fixes). Tree clean. Memory playbook gained two sections
("Embedder against internal headers", "Port-stub surface") + the
corrected CJS Module-config pattern.

## Turn 4 — User: "is the handoff ready right?"
**Assistant**: finalized the handoff's review status line, wrote this
recap, committed.

---

## Resume-from-here cheat sheet
- ACTIVE HANDOFF: docs/summaries/handoff-2026-06-09-phase3-canvas.md —
  Phase 3 plan: (1) canvas blit in a COOP/COEP tab (exported pixel-buffer
  fn or MODULARIZE + putImageData), (2) live RunLoop + rAF-driven repaint
  via ChromeClient invalidation, (3) input forwarding to EventHandler,
  (4) setScriptEnabled(true) — ONLY after the RunLoop is live, (5) images/
  scrolling. Known traps listed in the handoff.
- Gate rerun anytime: `./tools/build-webcore.sh && node tools/run-embedder.cjs`
- NEVER rm -rf build/webcore. Every WebKit edit → export-webkit-patches.sh.
- Single-threaded everywhere; one mutator thread per VM; local git only.
