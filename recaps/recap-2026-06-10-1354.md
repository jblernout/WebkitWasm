# Recap — 2026-06-10 13:54 (cookies session)

> Note: written immediately AFTER `/compact` fired (the compact interrupted the
> turn before a recap could be generated), reconstructed from the retained
> session summary. User messages are verbatim where preserved by the summary.

## Turn 1
**User:** "continue + wisp and the server itself are off rn might need to be
turned on if you need em" *(accompanied by a screenshot of the prior session's
wrap-up message tail)*

**Assistant:** Resumed the handoff's top priority: **COOKIES** (google.com
rendered the "Cookies are disabled" interstitial; document.cookie silently
no-opped). Started dev server (:8080) and wisp server (:5001).

Root cause **#10** in the empty-clients family: `pageConfigurationWithEmptyClients`
installs the real `CookieJar` over `EmptyStorageSessionProvider`, whose
`storageSession()` returns **nullptr** — DOM cookie writes vanished, reads
returned "". The network path additionally never attached `Cookie` headers nor
stored `Set-Cookie` responses.

Fix — entirely embedder-side, **zero WebKit patches**:
- `src/embedder/EmbedderStrategies.cpp`: singleton
  `NetworkStorageSession(defaultSessionID)` with
  `setCookieDatabase(CookieJarDB(":memory:"))` (in-memory sqlite, accept policy
  Always); `EmbedderStorageSessionProvider` returning it;
  `appendCookieHeader()` in `createCurlRequest` (on the COPY handed to
  CurlRequest — initial load + every redirect hop); `storeResponseCookies()`
  in `curlDidReceiveResponse` BEFORE the redirect check so 3xx legs store.
- `src/embedder/main.cpp`: `pageConfiguration.cookieJar =
  CookieJar::create(createEmbedderStorageSessionProvider())`.
- `tools/dev-server.mjs`: gate6 endpoints `/cookie-test/set`,
  `/cookie-test/redirect-set` (302+Set-Cookie), `/cookie-test/echo`
  (reflects bib* cookies only).
- `web/gate6/cookies.html` + `tools/gate6-cookies-test.mjs`: 4 pixel-signal
  checks (document.cookie round-trip, navigator.cookieEnabled, network
  set→echo, 3xx store+re-attach).

Codex review round:
- MEDIUM "redirects keep stale first-party" — **false alarm, verified**: our
  `m_loader->willSendRequest` chain reaches
  `DocumentLoader::willSendRequest`, which does
  `setFirstPartyForCookies(newRequest.url())` for main frames before our
  attach runs. Documented with a comment, no code change.
- MEDIUM test gap → added the 302-leg gate check (sigredir).
- LOWs applied: tolerant space-less `set-cookie:` parsing, bib*-filtered echo,
  cookie Path narrowed to /cookie-test.
- LOW declined: space-less raw-header gate variant (node http can't emit it).

Results: **gate6 4/4**, full suite **8/8 green**, **google.com renders its
real homepage in-engine** (build/diagnose-https-www-google-com.png).
old.reddit's "whoa there, pardner!" confirmed server-side (wisp-exit IP/UA),
not cookies. Commits: **aa5cda7** (6 files, +296) + **7edbe2f** (handoff hash
fix — lesson: never amend-in a file referencing the commit's own hash).
Handoff repointed to Phase 5 chrome (title/progress callbacks, back/forward
history). Memory files updated.

## Turn 2
**User:** `/compact midtask` then: "whats left before polishing, 1) abort on
https://fal.ai/grants and discord.com/login needs investigation, 2) site
support improvements 3) performance being pretty mid on most sites or having
bad input latency, worse cases it cant load the site or takes unreasonable
amounts of time 3.5) might be related to 3, google login specificly has 2
bugs, 1 the render is a bit wonky on entering your email its invisible/semi
covered and 2) after hitting enter it goes gray like its loading and then
nothing happens but continues to show frames so somethings wrong no aborts"

**Assistant:** (this session — investigation of the fal.ai/discord aborts,
then site support / performance / google-login bugs.)
