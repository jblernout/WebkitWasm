# Recap — 2026-06-10 session (canvas gate → PHASE 3 COMPLETE)

Self-contained chronological record. Prior session: recaps/
recap-2026-06-09-2358.md (Phase 2 gate — first offscreen paint under
node). Reader needs zero prior context beyond: this project compiles
WebKit to wasm (PORT=Emscripten, single-threaded, CLoop JSC, Skia CPU
raster, embedder against internal WebCore headers), and at session start
the engine had painted pixels offscreen but never in a browser tab.

---

## Turn 1 — Session resumed from compaction ("continue")
**Assistant** executed Phase 3 step 1 (canvas blit) end-to-end:
- Survey: RunLoopGeneric's `cycle()` is NON-BLOCKING for Iterate (the
  waitUntil is Drain-only) → safe to drive from rAF. Browser host pages
  CAN pre-set window.Module (the CJS shadowing trap is node-only).
  Single-threaded wasm memory is a plain ArrayBuffer → putImageData legal.
- main.cpp restructured: engine state in an intentionally LEAKED global
  `Engine` struct; gate mode unchanged; `Module.bibInteractive` (EM_ASM)
  switches to interactive mode → `Module.onEngineReady()` then
  `emscripten_exit_with_live_runtime()`. Exports: bib_frame_width/height,
  bib_tick (RunLoop::cycle), bib_render (readPixels → static unpremul
  RGBA buffer).
- tools/dev-server.mjs: repeatable `--mount /engine=build/webcore/bin`
  (realpath containment per mount) — no copying 87 MB per build.
- web/browser.html host page: preRun mkdirTree /var/cache/fontconfig,
  rAF loop tick→render→putImageData with FRESH heap view per frame
  (memory growth detaches), verdict from the engine's own bytes (canvas
  getImageData is premul-lossy).
- **GATE2-BROWSER: PASS first try** — identical counts (exactBlue=20000,
  redGlyph=1541), 11 sustained frames, crossOriginIsolated=true. Proof:
  build/gate2-canvas.png. Offscreen gate unregressed.
- Codex review: ZERO findings. Commit 0e97f19. Docs/memory updated.

## Turn 2 — User: "Whats left now"
**Assistant**: roadmap — Phase 3 remaining (invalidation, input,
script-on), Phase 4 networking (curl/Wisp/WebCrypto/threading risks),
Phase 5 chrome, Phase 6 perf. "After Phase 4 passes, it's genuinely
'browse real websites slowly'."

## Turn 3 — User: "finish phase 3"
**Assistant** built the full interactivity layer and fought three silent
root-cause bugs:
- **Built**: src/embedder/BibPageClients.h — BibChromeClient (all 4
  damage callbacks → one dirty flag; scheduleRenderingUpdate default
  false → WebCore's own rendering updates ride our RunLoop timer for
  free) + BibEditorClient (full EditorClient stub, EmptyEditorClient is
  final AND its should* gates return false which blocks ALL editing;
  WinCairo handleEditingKeyboardEvent crib with minimal command map).
  main.cpp: bib_mouse_move/button, bib_wheel (DOM deltas negated),
  bib_key (RawKeyDown/KeyUp/Char via ccall), bib_render(force)
  paint-if-dirty, Module.bibHTML host-supplied documents,
  focusController active/focused, script+scrollbars interactive-only.
  browser.html: 3 demo pages (hello/interactive/scripttest), input
  forwarding, __bib.probe(x,y). tools/gate3-browser-test.mjs: 8 pixel
  assertions via REAL Playwright input.
- **WebKit patches** (ledger ~1500 lines): EmptyClients.h scroll-damage
  callbacks final→override; PlatformKeyboardEventEmscripten gains
  disambiguateKeyDownEvent (libwpe crib, no m_handledByInputMethod —
  IME-port-guarded); 2.52 inline includes (NodeDocument.h,
  FrameDestructionObserverInlines.h).
- **Gate3 first run: hover PASSED, click/type/scroll FAILED** → script
  silently dead + wheel dead. Diagnostics exports (bib_diag,
  bib_scroll_to) added.
- **ROOT CAUSE 1 (script)**: pageConfigurationWithEmptyClients hardcodes
  SandboxFlags::all() (SVGImage must never script);
  Document::initSecurityContext copies frame flags → canExecuteScripts
  false regardless of Settings, console message eaten by empty client.
  Fix: clear LocalMainFrameCreationParameters.effectiveSandboxFlags in
  interactive mode. After: executeScript(6*7)=42 — JSC RUNS IN-PAGE.
- **ROOT CAUSE 2 (wheel)**: scroll_to worked, wheel returned handled=1
  with position frozen. Static tracing exhausted → temporary WTFLogAlways
  probes INSIDE WebCore (EventHandler/ScrollingEffectsController/
  ScrollAnimator) → smooth-scrolling branch taken:
  ScrollAnimator::scrollAnimationEnabled is only overridden under
  USE(COORDINATED_GRAPHICS); the BASE default returns TRUE → every wheel
  became an animation with NO display-refresh driver to tick it. Fix:
  guard-join `|| defined(__EMSCRIPTEN__)` (h+cpp) → Settings (false) →
  immediateScrollBy synchronous. Probes reverted before patch export.
- **User (verbatim, mid-debug)**: "the scrolling works for me just only
  if i click and drag, scrollbar doesnt work on scroll wheel or 2 finger
  trackpad drag" — live confirmation from the user's own tab; thumb-drag
  bypasses the animator, exactly matching the diagnosis.
- **GATE3-BROWSER: PASS 8/8** (initial colors, hover restyle, JS onclick,
  'h' keydown to page JS, typed "hi" through the editor into <input>,
  wheel scroll marker + scrolled-away). Offscreen + gate2 re-verified.
  Commit 3128f09. Brief: Phase 3 checked. Handoff rotated:
  phase3-canvas → docs/archive/; ACTIVE =
  docs/summaries/handoff-2026-06-10-phase4-networking.md. Memory playbook
  gained "Interactivity root causes".
- **Codex review**: 0 crit / 0 high / 1 med / 2 low. MED: Ctrl/Cmd combos
  forwarded into engine fields alongside host shortcuts → early-return.
  LOW: probe() bounds check; malloc null-check before stringToUTF8.
  All fixed, ALL THREE GATES re-verified green. Commit 3aaa18e.

## Turn 4 — Stale scheduled wakeup
Acknowledged as already-completed work (no-op).

## Turn 5 — User: "are handoff files and items pre compaction done and tldr whats left and what needs to be done"
**Assistant**: wrote this recap, confirmed the handoff package, gave the
remaining-work TLDR (see the active handoff for the full Phase 4 plan).

---

## Resume-from-here cheat sheet
- ACTIVE HANDOFF: docs/summaries/handoff-2026-06-10-phase4-networking.md
  — Phase 4 plan: (1) re-enable WebCore curl network sources (UserAgent/
  CurlSSLHandle/NetworkStateNotifier known-missing), (2) WebCrypto/
  BoringSSL decision (decision-003), (3) replace EmbedderLoaderStrategy
  (offline → real ResourceLoader path), (4) Wisp websocket transport
  (wisp-js WispWebSocket, libcurl.js pattern), (5) THREADING DECISION:
  CurlRequestScheduler thread vs single-threaded RunLoop pump vs pthread
  full rebuild (~35 min), (6) cookies/cache later.
- Gates: `node tools/run-embedder.cjs` (offscreen, byte-stable);
  dev server `node tools/dev-server.mjs web --mount
  /engine=build/webcore/bin`; then gate2/gate3-browser-test.mjs.
  Live demo: http://127.0.0.1:8080/browser.html
- Commits this session: 0e97f19 (canvas gate) → 3128f09 (Phase 3
  complete) → 3aaa18e (review fixes). Tree clean.
- NEVER rm -rf build/webcore. Every WebKit edit →
  tools/export-webkit-patches.sh. Single-threaded everywhere. Local git
  only. Memory playbook "Interactivity root causes" + "Live engine in a
  browser tab" are this session's recipes.
