# Recap — 2026-06-13→14 session: MotionMark regression → present-queue non-determinism fix + JSC crash localized

Self-contained chronological flow. Project: WebKit (WebCore+JSC) → wasm32, no-JIT CLoop,
in a browser tab; Skia GPU → `#screen`; Wisp networking; engine on an Emscripten pthread.
Active handoff: `docs/summaries/handoff-2026-06-14-present-fix-and-jsc-crash.md`.

---

**1. `/compact midtask` + "recap what was done whats left and why motion mark has fell so far
from the 100+ scores [img: MotionMark 18.32 @ 90fps] but it still feels decent? its werid".**
Recapped the no-JIT wall (research-05, JIT impractical; CLoop IC spike the one lead). Then
analyzed the **18.32**: MotionMark score = complexity *ceiling*, not smoothness; "feels decent"
because normal content sits below the ceiling. Diagnosed the drop as the **rcap=dynamic render
throttle** (landed earlier today) capping `updateRendering` to 33% of the thread once a pass
exceeds ~3.7ms; `ticks 102060 vs blits 22186` corroborated. Asked whether the "100+" was native
or a prior wasm build; offered an rcap A/B.

**2. "check docs memorys it was 100+ for a while... till we started doing changes" + "well rcap
fixed it oops [img: MotionMark 95.57 @ 144fps, rcap=off]".**
Confirmed: **rcap=dynamic was the regression — 95.57 (rcap=0) vs 18.32 (dynamic), ~5.2×.** Docs
ladder: **109.87** (2026-06-11 main-thread direct present) → ~95 (pthread+bitmap, −13%) → 18
(throttle). Surfaced the tension: rcap=0 reintroduces the Discord stutter the throttle fixed;
the throttle is too blunt. Offered throttle-fix options.

**3. "or just better dynamics so we dont start stuttering again but still have good scores when
needed" + "we did add the throttle for a reason i just thing having better maxes and mins".**
Read rcap constants (`kRcapUpdateBudget=0.33`, `kRcapMaxIntervalMs=33`). Argued a *static* budget
can't give both; proposed an input-recency-gated adaptive budget.

**4. "sometimes even with rcap 0 it doesnt stutter but i refresh and its back... cant replicate...
it just happens" + "make the loading popup less intrusive" + "inputs is kinda a bad measurement".**
Agreed input is a bad signal; reframed the intermittency as a race/pacing pathology; proposed
gating on paint-progress/cadence instead. **Shipped the popup:** `#bibfreeze` overlay → small
bottom-right corner badge, no canvas dim (`web/browser.html`, host-side, no rebuild).

**5. "did any of our past agents and chats already do something similar".**
Found **`build/present-cadence.mjs`** already measures delivered-frame cadence + **cv** with
`BIB_RCAP`/`BIB_INFLIGHT` knobs — and was used earlier today to set rcap (rcap=30: cv 0.60;
rcap=0: cv 1.92). Identified the blind spot: prior A/B tested Discord + a *light* vsync-capped
isolator (rcap no-op there), never a heavy-but-smooth page → that's how the MotionMark regression
shipped invisibly.

**6. "sure run it" (measure MotionMark cv).**
Wrote `build/motionmark-cadence.mjs` (clicks the in-canvas Run button via `bib_click`). Button is
below the fold → scroll 350 + click (0.25,0.73). First runs LOOKED identical (cv 1.98) → caught via
screenshot that **MotionMark hadn't started** (missed click). Fixed; a valid `rcap=0` run = cv 1.57,
35.5fps. Concluded MotionMark cadence is confounded by its own start-stop subtest structure; the
real story is the score↔delivery split.

**7. "tldr".** Tight summary of the present/throttle state.

**8. "have 5.5 pro + 10 key files... best solution, bugs, perf, 'how to make webkit-in-wasm as fast
as possible with least tradeoffs'".**
AskUserQuestion → **Granted.** Wrote `build/gpt55-perf-brief.md`, launched GPT-5.5 Pro (background)
with 10 files. Returned a strong consult → saved **`docs/research/research-06-perf-present-consult.md`**.
**Headline (I verified in code):** our "latest-wins" present is actually **bounded FIFO depth-3 +
early-ack** (`engine-pre.js:157 maxInFlight=3`; `browser.html:170` returns credit before `:173`
`transferFromImageBitmap`) = the likely non-determinism. Also: delivery-first controller; present-
before-update; the two-modes bound.

**9. "tldr".** Compressed the consult: present bug + fix (strict-1+tick-on-ack), delivery-first
throttle, two-modes, inflight=1/2/3 test next.

**10. "sure" (run the inflight matrix).**
Headless fell back to SwiftShader→raster (bypasses the queue, useless). Ran **headed real-GPU**:
- rcap=0: inflight=1 → 17-38fps; inflight=3 → **0.1-3fps near-frozen**.
- rcap=dynamic: inflight=1 → cv 0.76/0.78/0.78 (STEADY); inflight=3 → 0.62/0.73/**4.26** (BIMODAL,
  one near-frozen).
**Conclusion: depth-3 + early-ack floods the page main thread → the random freeze. inflight=1
eliminates it.**

**11. "ill just eyeball it".**
Flipped `web/browser.html` present default **maxInFlight 3 → 1** (host-side, `?inflight=N`
overrides). Gave an eyeball checklist (watch hard-graphics for a lock-to-1fps dead-gap). Launched
**Codex review** (background).

**12. [crash log: `materializePropertyTable` abort at rcap=0] + "on rcap=0 its just still stuttery
so idk".** Split it: the crash is a pre-existing JSC bug (host-side change can't cause it); rcap=0
stutter is expected (saturation bound) — smoothness lives in dynamic.

**13. [Codex returned]** Q1-Q4 clean (ordering/clamp/always-send/credit-accounting safe); **Q5
MEDIUM = tick-on-ack needed** (ready handler doesn't reschedule a tick → ~16ms dead-gap on
hard-graphics) — independently confirming GPT-5.5.

**14. "what im so confused... rcap0 stuttery+high scores, dynamic smooth+low scores, what did this
change fix?".** Clarified **two separate knobs:** rcap (score↔smoothness) vs inflight (present
queue / non-determinism). The change fixed the **random freeze**, not the score tradeoff.

**15. "uh no... i never really had random freeze on dynamic... and this error is happening a lot
more now its really annoying [crash log #2: materializePropertyTable 39 vs 38]".**
Owned the mis-framing (deferred to their experience: dynamic was fine; rcap=0 was the inconsistent
one). Prioritized the crash. Gave `&inflight=3` isolation test. Explained the **off-by-one** and
located the abort (`Structure.cpp:483`).

**16. "dont make any edits yet".**
Read `materializePropertyTable` (Structure.cpp:419-496): rebuilds the property table by replaying
the structure transition chain; **PropertyDeletion path (`:462-468`) is the prime suspect** for the
off-by-one. Held all edits.

**17. "how easy or hard is the change".**
Scoped it. Found: build is `Release` and the check **still fires**; `checkOffsetConsistency`
(StructureInlines.h:433) is **ALWAYS_INLINE, not assertion-gated**; WebKit pin `aec9d2ad95`
(2026-06-07) is near head. → **both cheap escapes ruled out** (no build-flag dodge; "backport a
known fix" window is ~1 week). Verdict: **medium-hard, leaning hard** — likely a real bug from our
config (no-JIT/wasm/pthread) or our patches; consistent off-by-one argues logic over corruption.

**18. "make a plan handoff so i can compact and this can be fixed".**
Wrote `docs/summaries/handoff-2026-06-14-present-fix-and-jsc-crash.md` (full crash fix plan +
present/throttle state + uncommitted/commit guidance) and this recap.

---

**State at recap.** HEAD unchanged (commits only when asked). Uncommitted: M `web/browser.html`
(corner badge + inflight=1), M `src/embedder/main.cpp` (BIBPERF, prior session, in live binary),
?? `docs/research/research-04/05/06`, ?? `build/motionmark-cadence.mjs`. Engine binary = 17:54
build (NOT rebuilt this session). **Shipped:** present default inflight=1 (kills random freeze) +
corner-badge overlay. **Next:** fix the `materializePropertyTable` JSC crash (handoff Part A);
then `tick-on-ack` relink + delivery-first throttle. Score↔smoothness = single-thread bound (two
modes). Crash likely same family as **#57**.
